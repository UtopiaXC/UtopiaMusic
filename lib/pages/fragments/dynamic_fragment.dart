import 'package:flutter/material.dart';

class DynamicFragment extends StatelessWidget {
  const DynamicFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('动态内容'));
  }
}
